import {vanillaExtractRollupPlugin} from "../src";

describe("vanilla-extract rollup plugin", () => {
  it("should be defined", () => {
    expect(vanillaExtractRollupPlugin).toBeDefined();
  });
});
